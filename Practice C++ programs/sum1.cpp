// Find Sum of two user-defined numbers.
#include <iostream>
using namespace std;
int main()
{
    int x,y,sum;
    cout<<"Enter two numbers : ";
    cin>>x>>y;
    sum=x+y;
    cout<<"The sum of "<<x<<" and "<<y<<" is "<<sum<<"\n";
    return 0;
}
